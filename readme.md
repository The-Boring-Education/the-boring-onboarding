# React-typescript-tailwind-boilerplate
Kickstarter for React typescript and tailwind project

## Analytics

- Set `VITE_GA_MEASUREMENT_ID` in `.env` or `.env.local` to enable GA4 tracking.
- Events tracked include page views, clicks, form submissions, and onboarding step navigation/complete.
